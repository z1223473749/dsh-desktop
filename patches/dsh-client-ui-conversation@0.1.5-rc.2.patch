diff --git a/lib/client.js b/lib/client.js
--- a/lib/client.js
+++ b/lib/client.js
@@ -2700,7 +2700,7 @@
 		//#endregion
 		//#region lib/types/client/stores.js
 		/** Per-session Conversation store shared by the shell body and header. */
-		const CONVERSATION_STORE_KEY = "dsh.conversation";
+		const CONVERSATION_STORE_KEY = globalThis.__DSH_SESSION_WINDOW_TARGET__ ? "dsh.session-window.conversation" : "dsh.conversation";
 		/**
 		* Declare per-session draft persistence and View selection.
 		* @returns the store handle.
@@ -15268,7 +15268,7 @@
 					return true;
 				}
 				event.preventDefault();
-				handlers.pasteText(text);
+				handlers.pasteText(text, Array.from(clipboardData.items).some((item) => item.kind === "file"));
 				return true;
 			}, 4));
 		}
@@ -15953,7 +15953,7 @@
 				running,
 				steeringAvailable,
 				busyEnter,
-				intakeFiles,
+				addFiles, canAcceptDrop, intakeFiles,
 				uploadsPending,
 				showToast,
 				t
@@ -15965,7 +15965,7 @@
 				running,
 				steeringAvailable,
 				busyEnter,
-				intakeFiles,
+				addFiles, canAcceptDrop, intakeFiles,
 				uploadsPending,
 				showToast,
 				t
@@ -15997,9 +15997,9 @@
 					intakeFiles: (files) => {
 						gate.current.intakeFiles(files);
 					},
-					pasteText: (text) => {
+					pasteText: (text, hasClipboardFiles) => {
 						if (gate.current.machineBusy || gate.current.locked) return;
-						keyboard.paste(text);
+						if (!hasClipboardFiles && gate.current.canAcceptDrop && gate.current.addFiles && window.__DSH_DESKTOP_PASTED_TEXT_FILE__?.(text, gate.current.addFiles)) return; keyboard.paste(text);
 					}
 				});
 			}, [editor, keyboard]);
