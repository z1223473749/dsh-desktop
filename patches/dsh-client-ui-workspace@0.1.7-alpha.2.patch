diff --git a/lib/client.js b/lib/client.js
--- a/lib/client.js
+++ b/lib/client.js
@@ -933,6 +933,7 @@
 				return result.value;
 			}
 			watchNavigation() {
+				if (globalThis.__DSH_SESSION_WINDOW_TARGET__) return () => { this.lifetime.abort(); };
 				let initial = "waiting";
 				const reconcile = () => {
 					if (this.lifetime.signal.aborted) return;
@@ -1354,11 +1355,11 @@
 							onClose: () => {
 								setMenuOpen(false);
 							},
-							items: workspaceMenuItems,
+							items: [...workspaceMenuItems.slice(0, 1), ...(window.__DSH_DESKTOP_ARCHIVE_WORKSPACE__ ? [{ id: "desktop-archive", label: window.__DSH_DESKTOP_ARCHIVE_WORKSPACE__.label(), icon: (0, react_jsx_runtime.jsx)(_deepseek_ai_dsh_client_ui_primitives.IconArchiveOutlineRegular, { size: 16 }) }] : []), ...workspaceMenuItems.slice(1)],
 							onSelect: (id) => {
 								setMenuOpen(false);
 								/* v8 ignore next -- Menu can emit only the rename and delete rows supplied above. */
-								if (id !== "rename" && id !== "delete") return;
+								if (id === "desktop-archive") { window.__DSH_DESKTOP_ARCHIVE_WORKSPACE__?.archive(group.workspaceId); return; } if (id !== "rename" && id !== "delete") return;
 								if (id === "rename") actions.rename();
 								else actions.delete();
 							},
@@ -1634,6 +1635,7 @@
 					role: "treeitem",
 					"aria-selected": selected,
 					"aria-description": row.archived ? t("toast.archivedNotOpenable") : void 0,
+					onContextMenu: (e) => { if (!row.blank && (window.__DSH_DESKTOP_ARCHIVE_WORKSPACE__ || globalThis.__DSH_SESSION_WINDOW_UI__)) { e.preventDefault(); e.stopPropagation(); setMenuOpen(true); } },
 					onClick: () => {
 						onOpen(node.id);
 					},
@@ -1641,11 +1643,12 @@
 					onPointerLeave: marquee.leave,
 					draggable,
 					onDragStart: !draggable ? void 0 : (e) => {
-						e.dataTransfer.effectAllowed = "move";
+						if (globalThis.__DSH_SESSION_WINDOW_UI__ && !globalThis.__DSH_SESSION_WINDOW_UI__.start(node.id)) { e.preventDefault(); return; }
+					e.dataTransfer.effectAllowed = "move";
 						e.dataTransfer.setData("text/plain", node.id);
 						drag.start();
 					},
-					onDragEnd: !draggable ? void 0 : drag.end,
+					onDragEnd: drag === void 0 ? void 0 : (e) => { drag.end(globalThis.__DSH_SESSION_WINDOW_UI__?.end(node.id, e) === true); },
 					onDragOver: drag === void 0 ? void 0 : (e) => {
 						if (!drag.active) return;
 						e.preventDefault();
@@ -2580,8 +2583,8 @@
 										half: normalizeHalf(half)
 									});
 								},
-								end: () => {
-									if (drag?.over !== null && drag?.over !== void 0) commitSessionDrag(drag, drag.over);
+								end: (cancel = false) => {
+									if (!cancel && drag?.over !== null && drag?.over !== void 0) commitSessionDrag(drag, drag.over);
 									else setDrag(null);
 									sessionDropCommitted.current = false;
 								}
@@ -2715,8 +2718,8 @@
 										half: normalizeHalf(half)
 									});
 								},
-								end: () => {
-									if (drag?.over !== null && drag?.over !== void 0) commitDrag(drag, drag.over);
+								end: (cancel = false) => {
+									if (!cancel && drag?.over !== null && drag?.over !== void 0) commitDrag(drag, drag.over);
 									else setDrag(null);
 									dropCommitted.current = false;
 								}
