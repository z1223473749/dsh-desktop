diff --git a/lib/runner-launch-B2zsQ1Dz.js b/lib/runner-launch-B2zsQ1Dz.js
index 538e414..20cc2c4 100644
--- a/lib/runner-launch-B2zsQ1Dz.js
+++ b/lib/runner-launch-B2zsQ1Dz.js
@@ -1347,6 +1347,14 @@ function runnerEnvironment(selection, invocation) {
 		const normalized = name.toUpperCase();
 		if (RUNNER_CONTROL_ENV_PREFIXES.some((prefix) => normalized.startsWith(prefix))) Reflect.deleteProperty(env, name);
 	}
+	// Electron hosts must start the private runner in Node mode on every
+	// platform; target env arrives over IPC, so never add this flag to it.
+	if (process.versions.electron !== void 0 && (process.platform !== "win32" || selection === WINDOWS_RUNNER_SELECTION)) {
+		for (const name of Object.keys(env)) {
+			if (name.toUpperCase() === "ELECTRON_RUN_AS_NODE") Reflect.deleteProperty(env, name);
+		}
+		env.ELECTRON_RUN_AS_NODE = "1";
+	}
 	return {
 		...env,
 		[SUBPROCESS_RUNNER_ENV]: selection,
